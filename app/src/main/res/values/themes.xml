<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base de la aplicación -->
    <style name="Base.Theme.Rush" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/enterprise</item>
        <item name="colorPrimaryDark">@color/orange</item>
        <item name="colorAccent">@color/marron</item>
    </style>
    <style name="CustomBottomNavigationStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:itemBackground">@color/marron</item>
    </style>

    <!-- Tema para el modo claro -->
    <style name="Theme.Rush" parent="Base.Theme.Rush" />
</resources>
